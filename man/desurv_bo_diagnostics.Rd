% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/desurv_bo_diagnostics.R
\name{desurv_bo_diagnostics}
\alias{desurv_bo_diagnostics}
\title{Diagnostics for Bayesian optimisation runs}
\usage{
desurv_bo_diagnostics(bo_result, include_plots = TRUE)
}
\arguments{
\item{bo_result}{Object produced by \code{\link[=desurv_bayesopt]{desurv_bayesopt()}} or
\code{\link[=desurv_cv_bayesopt]{desurv_cv_bayesopt()}}.}

\item{include_plots}{Logical; if \code{TRUE}, return \code{ggplot2} objects for
inspection. Plots are skipped when \code{ggplot2} is not installed.}
}
\value{
A list with components:
\itemize{
\item \code{summary}: tibble-like single-row data frame of aggregate metrics
(evaluation counts, runtime, best score).
\item \code{progress}: data frame with the cumulative best score per evaluation.
\item \code{sensitivity}: data frame describing relative importance estimates
derived from the GP length-scales. \code{NULL} when the surrogate was
unavailable.
\item \code{plots}: named list of \code{ggplot} objects (or \code{NULL} when plotting is
disabled).
}
}
\description{
Summarise the evaluation history returned by \code{\link[=desurv_bayesopt]{desurv_bayesopt()}} or
\code{\link[=desurv_cv_bayesopt]{desurv_cv_bayesopt()}} and create lightweight diagnostic plots, including a
hyperparameter-sensitivity proxy derived from the final Gaussian-process
surrogate.
}
\examples{
\dontrun{
bo_fit <- desurv_bayesopt(...)
diag <- desurv_bo_diagnostics(bo_fit)
diag$plots$progress
diag$plots$sensitivity
}

}
