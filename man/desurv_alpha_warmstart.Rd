% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/desurv_alpha_warmstart.R
\name{desurv_alpha_warmstart}
\alias{desurv_alpha_warmstart}
\title{Warm-start DeSurv over alpha for multiple random initializations (optional parallel)}
\usage{
desurv_alpha_warmstart(
  X,
  y = NULL,
  d = NULL,
  k = NULL,
  alpha_grid,
  lambda,
  nu,
  lambdaW,
  lambdaH,
  n_starts = 5,
  seed = 123,
  tol = 1e-06,
  maxit = 1000,
  parallel = FALSE,
  ncores = NULL,
  verbose = TRUE
)
}
\arguments{
\item{X}{Either a numeric matrix (\eqn{p \times n}) of nonnegative features
(e.g., genes by samples), or a \code{"desurv_data"} object created by
\code{\link{desurv_data}}.}

\item{y}{Numeric vector of survival times of length \eqn{n}. Used only if
\code{X} is a matrix or data frame; ignored if \code{X} is a
\code{"desurv_data"} object.}

\item{d}{Numeric (0/1) vector of event indicators of length \eqn{n}.
Used only if \code{X} is a matrix or data frame; ignored if \code{X}
is a \code{"desurv_data"} object.}

\item{k}{Positive integer rank (number of latent factors). Used only if
\code{X} is a matrix or data frame; ignored if \code{X} is a
\code{"desurv_data"} object (where \code{k} is already stored).}

\item{alpha_grid}{Numeric vector of supervision parameter values to explore.}

\item{lambda}{Numeric non-negative scalar; global penalty parameter.}

\item{nu}{Numeric scalar in \eqn{[0, 1]} giving the elastic-net mixing
parameter for \eqn{\beta}.}

\item{lambdaW}{Numeric non-negative scalar; penalty parameter applied to
the \code{W} factor.}

\item{lambdaH}{Numeric non-negative scalar; penalty parameter applied to
the \code{H} factor.}

\item{n_starts}{Integer number of independent random initialization paths.}

\item{seed}{Integer or \code{NULL}; base seed for reproducibility.}

\item{tol, maxit}{Numeric; optimizer tolerance and max iterations per fit.}

\item{parallel}{Logical; if \code{TRUE}, run different initialization paths
in parallel using \code{parallel::mclapply()} (Unix/macOS only).}

\item{ncores}{Integer or \code{NULL}; number of cores to use when
\code{parallel = TRUE}. Defaults to \code{parallel::detectCores()} capped
at \code{n_starts}.}

\item{verbose}{Logical; print progress.}
}
\value{
A list with:
\itemize{
\item \code{results}: data frame with \code{init_id}, \code{alpha}, \code{cindex}.
\item \code{fits}: list of all \code{"desurv_fit"} objects by path and alpha.
\item \code{alpha_grid}: vector of alpha values used.
}
}
\description{
Run DeSurv fits over a sequence of supervision parameters \code{alpha_grid}
using warm starts, and repeat this for multiple random initializations.
For each initialization path, a single random initialization
\eqn{(W_0, H_0, \beta_0)} is drawn, and a full warmstart trajectory is fit
over \code{alpha_grid}. Paths can optionally be executed in parallel.
}
