% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/desurv_bo_refine.R
\name{desurv_bo_refine_bounds}
\alias{desurv_bo_refine_bounds}
\title{Refine Bayesian optimisation bounds based on prior history}
\usage{
desurv_bo_refine_bounds(
  bounds,
  history,
  top_k = 10,
  importance = NULL,
  shrink_base = 0.5,
  importance_gain = 0.3
)
}
\arguments{
\item{bounds}{Named list describing the current search space (same format
as \code{bo_bounds} passed to \code{\link[=desurv_bayesopt]{desurv_bayesopt()}} or \code{\link[=desurv_cv_bayesopt]{desurv_cv_bayesopt()}}).}

\item{history}{Data frame of past evaluations containing the parameter
columns plus \code{mean_cindex} and \code{status}.}

\item{top_k}{Integer; number of best evaluations to consider when defining
the allowable refined range. Defaults to 10.}

\item{importance}{Optional named numeric vector of parameter importances
scaled to \verb{[0, 1]}. Names must match the parameter keys in \code{bounds}. When
\code{NULL}, all parameters are treated equally.}

\item{shrink_base}{Baseline shrinkage strength between 0 and 1. Values
closer to 1 push the bounds quickly towards the top-k envelope.}

\item{importance_gain}{Additional shrinkage applied to each parameter
according to its importance score (0-1). For example, with
\code{importance_gain = 0.5}, a parameter with \code{importance = 1} will shrink
\verb{50\%} faster than the base rate.}
}
\value{
A list of refined bounds ready to be passed back into the BO helper.
}
\description{
Shrinks an existing set of BO bounds using the top-performing evaluations
from a previous run while respecting the original search space. Parameter
importance scores can be supplied to tighten high-impact parameters more
aggressively than low-impact ones.
}
