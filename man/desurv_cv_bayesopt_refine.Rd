% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/desurv_bo_refine.R
\name{desurv_cv_bayesopt_refine}
\alias{desurv_cv_bayesopt_refine}
\title{Sequentially refine the BO search space for desurv_cv()}
\usage{
desurv_cv_bayesopt_refine(
  X,
  y,
  d,
  dataset = NULL,
  samp_keeps = NULL,
  preprocess = TRUE,
  method_trans_train = c("rank", "quant", "none"),
  engine = c("cold", "warmstart"),
  coarse_bounds,
  bo_fixed = list(),
  max_refinements = 3,
  tol_gain = 0.002,
  plateau = 1,
  top_k = 10,
  shrink_base = 0.5,
  importance_gain = 0.3,
  coarse_control = list(),
  refine_control = list(),
  verbose = TRUE,
  ...
)
}
\arguments{
\item{X, y, d, dataset, samp_keeps}{Inputs forwarded to \code{\link[=desurv_cv]{desurv_cv()}}. Provide
\code{dataset}/\code{samp_keeps} when preprocessing is enabled.}

\item{preprocess, method_trans_train, engine}{Arguments passed through to
\code{\link[=desurv_cv]{desurv_cv()}}. Bayesian optimisation assumes the output metric is
comparable across iterations, so keep these unchanged throughout the run.}

\item{coarse_bounds}{Initial search ranges supplied to the coarse BO run.}

\item{bo_fixed}{Named list of fixed hyperparameters (forwarded to
\code{\link[=desurv_cv_bayesopt]{desurv_cv_bayesopt()}}).}

\item{max_refinements}{Maximum number of refinement rounds after the coarse
run.}

\item{tol_gain}{Minimum improvement in best mean C-index required to
continue refining.}

\item{plateau}{Runs without improvement before stopping early.}

\item{top_k}{Number of top evaluations to define the refined envelope.}

\item{shrink_base}{Baseline shrinkage factor passed to
\code{\link[=desurv_bo_refine_bounds]{desurv_bo_refine_bounds()}}.}

\item{importance_gain}{Additional shrink factor applied using parameter
importances.}

\item{coarse_control}{Optional named list of arguments overriding the coarse
BO call (e.g., \code{list(n_init = 10, n_iter = 20)}).}

\item{refine_control}{Optional named list applied to each refined BO call.}

\item{verbose}{Logical; print progress after each evaluation/iteration.}

\item{...}{Additional arguments forwarded to \code{\link[=desurv_cv]{desurv_cv()}} (for example
\code{folds}, \code{parallel_grid}, \code{ncores_grid}).}
}
\value{
An object of class \code{desurv_cv_bo_refine} containing every run,
combined history, the final bounds and the overall best parameters.
}
\description{
Sequentially refine the BO search space for desurv_cv()
}
